# Sao chép thành .env.local và chỉnh nếu FastAPI không chạy ở localhost:8000
VITE_API_BASE_URL=http://localhost:8000
# Tên file trong web/states/ (không .txt). Có thể ghi đè bằng ?state= trên URL
# VITE_INITIAL_STATE=a_star
